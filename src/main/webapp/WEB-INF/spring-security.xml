<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">
 
	<!-- enable use-expressions -->
	<http auto-config="true" use-expressions="true">
 
		<!-- access denied page -->
		<!-- <access-denied-handler error-page="/403" /> -->
		<form-login 		
		    login-page="/login" 
		    authentication-success-handler-ref="roleBasedRedirect"
		    default-target-url="/"
			authentication-failure-url="/login?error" 
			username-parameter="email"
			password-parameter="password" />
			
		<logout logout-success-url="/login" invalidate-session="true" delete-cookies="true" logout-url="/logout" />
		
		<!-- enable csrf protection -->
		<csrf />
		
		
		<intercept-url pattern="/resources/**" access="permitAll"/>
		<intercept-url pattern="/admin/super/**" access="hasRole('Admin_Super')" />
		<intercept-url pattern="/user/**" access="hasRole('User_Free')" />
		<intercept-url pattern="/" access="permitAll"/>
		<intercept-url pattern="/login" access="permitAll"/>
		<intercept-url pattern="/register" access="permitAll"/>
		<intercept-url pattern="/j_spring_security_logout" access="permitAll"/>
		<intercept-url pattern="/logout/**" access="permitAll"/>
		<intercept-url pattern="/forgotPassword" access="permitAll"/>
		<intercept-url pattern="/activateAccount" access="permitAll"/>
		<intercept-url pattern="/studyRoom/**" access="permitAll"/>
		<intercept-url pattern="/**" access="denyAll"/> 
		
	</http>
 
	<authentication-manager>
		<authentication-provider user-service-ref="authenticationService" >
			<password-encoder hash="bcrypt" />
		</authentication-provider>
	</authentication-manager>
	 
	 
</beans:beans>